<%= simple_form_for  ([@topic, @thought]) do |f| %>
    <%= f.input :content, as: :text, rows: 8 %>
    <%= f.button :submit %>
    <%= link_to 'Destroy', [@topic , @thought] , method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>